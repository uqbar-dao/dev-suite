/+  *zig-sys-smart
|%
+$  action
  $%  $:  %deploy
          mutable=?
          code=[bat=* pay=*]
          interface=(map @tas json)
          types=(map @tas json)
      ==
      ::
      $:  %upgrade
          to-upgrade=id
          new-code=[bat=* pay=*]
      ==
  ==
--
